server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:urlshortener
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: updata
    show-spl: ture
  h2:
    console:
      enabled: true
app:
  base-url: http://localhost:8080/